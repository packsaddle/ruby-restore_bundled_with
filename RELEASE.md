# Release

## Prepare

* Bump version `lib/restore_bundled_with/version.rb`
* Update changelog
    * `$ npm install`
    * `$ npm run changelog`
    
## Release

* `$ bundle exec rake release`

DONE!
