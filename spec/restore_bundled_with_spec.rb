require 'spec_helper'

describe RestoreBundledWith do
  it 'has a version number' do
    expect(RestoreBundledWith::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
